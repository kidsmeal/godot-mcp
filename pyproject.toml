[project]
name = "godot-grounding-mcp"
version = "0.1.0"
description = "Version-exact Godot API + project-convention grounding MCP server"
requires-python = ">=3.11"
dependencies = ["mcp>=1.12"]

[tool.setuptools]
package-dir = {"" = "src"}
